# AVM Planner

GitHub Pages deployment trigger.

The production page is built from `planner.b64` during the Pages workflow so the browser does not need to fetch the compressed planner asset at runtime.
